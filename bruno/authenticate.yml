info:
  name: authenticate
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/authenticate
  body:
    type: json
    data: |-
      {
          "username": "admin",
          "password": "admin"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
